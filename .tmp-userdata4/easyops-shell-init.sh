# EasyOps runtime init (auto-generated — do not edit)
# Disable tty echo so the injected script is not echoed back to the terminal.
stty -echo 2>/dev/null || true
# Minimal clean prompt for the interactive session after the script finishes.
PS1='$ '

# 2026-08-01 工作日志

## EasyOps 基础架构 + 日志模块（第一步）

- 确立分层架构：渲染层(React+Vite+xterm) → Preload(IPC) → 主进程(Electron + PTY Host) → 后端服务(Express + scripts.json) → 共享日志模块。
- 交付 `shared/logger.js`：createLogger 工厂，开发模式输出终端、生产模式写文件（JSON Lines）；支持级别阈值、Error 自动展开 stack、context 上下文、installProcessHandlers 进程兜底；close() 返回 Promise 确保落盘。
- 交付 `server/config.js`：集中后端可配置项，日志默认目录优先级为 ENV(EASYOPS_LOG_DIR) > 代码默认值（回退 ~/.easyops/logs），满足"默认路径后端可配置"。
- 交付 `ARCHITECTURE.md`（含数据流与日志配置表）与 `scripts/verify-logger.js` 自检脚本，已用 node 实际运行通过（dev 过滤 debug、prod 写文件含 stack+context）。
- 骨架：`package.json`、`electron/main.js`、`server/index.js`、`electron/pty-host.js`（含 logger.child({scriptId}) 上下文接入示例）。

## 客户端 UI 设计（第二步）

- 按三张截图交付：顶部按钮区 / 左侧分组脚本列表 / 右侧执行输出面板，每个脚本独立输出卡。
- Vite 5 + React 18，root=`client/`、构建产物输出到根 `dist/`。
- 组件：`TopBar`、`ScriptList`(分组表头/列头/行)、`ScriptItem`(拖拽柄+勾选+名称+状态+Execute/Edit/Delete/Locate)、`ExecutionPanel`、`ExecutionCard`(BE/FE 徽章+耗时+相对时间+Exit+贴底+重跑+最大化+Close+独立滚动输出)。
- 自定义滚动条（webkit，浅灰 thumb）、彩色动作按钮、状态徽章配色对齐截图。
- Mock 数据 + 模拟流式输出，进入应用自动跑 PMS-后端-TEST / PMS-后端-DEV 复现截图运行态。
- 已 `npm install --ignore-scripts`，Vite dev server 在 5173 运行（curl 200，关键模块编译通过）。
- 后续步骤：新增/编辑脚本表单、后端 /api/scripts CRUD 接入、PTY 双向流到 ExecutionCard 输出区。

## 依赖版本对齐修正（AGENTS.md 约束）

- AGENTS.md 硬性规定版本：electron 43.2.0 / node-pty 1.1.0 / xterm.js 6.0.0 / xterm/addon-fit 0.11.0。
- 之前错用 `@xterm/xterm@^5.5.0` 且漏装 addon-fit；已改为 `@xterm/xterm@6.0.0` + `@xterm/addon-fit@0.11.0`（钉死精确版本，不用 caret）。
- 关键教训：xterm v6 的正确包名是 `@xterm/xterm`（无作用域的 `xterm` 最高仅 5.3.0）；fit 插件正确包名是 `@xterm/addon-fit`（不是 `xterm-addon-fit` 也不是 `@xterm/xterm-addon-fit`）。
- `package.json` 中 electron / node-pty / @xterm/xterm / @xterm/addon-fit 均已钉死；重装后 node_modules 实际版本与 AGENTS.md 完全一致。ExecutionCard 目前仍是 mock 输出，尚未真正接入 xterm v6，待后续 PTY 双向流步骤落地。

## vite 版本对齐（AGENTS.md 新增 vite v8.2.0）

- 用户更新 AGENTS.md：新增 `- vite v8.2.0`（第 10 行）。原 package.json 锁 `vite ^5.4.0` 不符，已钉死为 `vite "8.2.0"`。
- vite 8 的 peer 要求：Node `^20.19.0 || >=22.12.0`（本机托管 Node 22.22.2 满足）；`@vitejs/plugin-react` 必须从 v4 升到 v6 才能配 vite 8（最新 6.0.5，peer `vite: ^8.0.0`），否则 peer 冲突、dev server 起不来。已同步改为 `@vitejs/plugin-react "^6.0.5"`。
- 重装后实测：`vite@8.2.0` + `@vitejs/plugin-react@6.0.5`，`npm ls` 无 peer 冲突；`npm run dev` 启动 HTTP 200，main.jsx/App.jsx/ExecutionCard.jsx/vite.config.js 编译均 200，无配置破坏性变更。验证后已按用户上一步"中断执行"要求停掉 dev server，端口 5173 释放。

## AGENTS.md 依赖再次对齐（^v 约定 + react 19）

- AGENTS.md 更新：所有依赖版本写法统一为 `^v` 前缀（caret 范围），并新增 `react ^v19.2.8`。
- package.json 同步：electron/node-pty/@xterm/xterm/@xterm/addon-fit/vite 由精确钉死改为 `^` caret；react/react-dom 由 `^18.3.1` 升到 `^19.2.8`（react-dom 必须同主版本，否则与 react 19 冲突）。
- 实测：`npm ls` 无 peer 冲突；React 19 + vite 8 下 `npm run dev` 启动 HTTP 200，main.jsx/App.jsx/ExecutionCard.jsx/TopBar.jsx 编译均 200（客户端用 createRoot + 函数组件/hooks，无 React 19 破坏性变更影响）。验证后停掉 dev server，端口释放。
- 结论：当前 package.json 与 AGENTS.md 全部 7 项依赖（electron/node-pty/xterm.js/xterm-addon-fit/vite/react + react-dom 配套）版本范围一致。

## AGENTS.md 再次对齐（新增 electron-builder 26）

- AGENTS.md 新增 `electron-builder ^v26.15.3`（第 12 行）。package.json 原 `^24.13.3` → `^26.15.3`。
- 实测：`npm ls` 无 peer 冲突（electron-builder 26 无 electron peer 强约束），实际安装 26.15.3。
- 至此 package.json 与 AGENTS.md 全部 8 项依赖（electron/node-pty/xterm.js/xterm-addon-fit/vite/react/react-dom 配套/electron-builder）版本范围完全一致。

## 客户端：Add Group 静态界面

- 新增 `client/src/components/AddGroupModal.jsx`：模态框 + Group Name 输入 + 实时校验（min 1 / max 6 字符，maxLength=6 硬拦截；重名校验 `existing.includes`）+ 字符计数 `n/6` + Save/Cancel（校验不过禁用 Save）。UI 全英文（AGENTS.md 约定）。
- TopBar 新增 `Add Group` 按钮（样式对齐 Add Script），通过 `onAddGroup` 打开模态框。
- App 新增 `groups` 本地 state（初始 BACKEND/Frontend SCRIPTS）+ `addGroupOpen` + `handleSaveGroup`（仅写本地 state，空分组，不接后端，符合"先静态"）。
- ScriptList 改为接收显式 `groups` prop 渲染（不再从 scripts 派生），空分组显示 "No scripts in this group" 占位行；分组级勾选在空分组置灰。
- styles.css 新增模态框/字段/ghost 按钮/空分组占位样式，复用现有配色 token。
- 验证：`npm run dev` 启动 HTTP 200，main/App/AddGroupModal/TopBar/ScriptList 编译均 200，转译产物含 AddGroupModal/groups 接线与 MAX_LEN/MIN_LEN/重名校验。验证后停掉 dev server。
- 后续：Add Group 接后端（/api/groups 持久化）、Add Script 表单、PTY 双向流。

## Vite 黄字警告修复

- 现象：`vite.config.js` 用 ESM(import/export) 但项目无 `"type":"module"`，被当 CommonJS 加载，报 `configLoader: 'native'` 警告。
- 修法：重命名为 `vite.config.mjs`（Vite 原生候选名）；未改 package.json 的 `"type"`，以免破坏项目内 CommonJS 文件(shared/logger.js、server/config.js、electron/main.js、scripts/verify-logger.js 均用 require)。
- 验证：`npm run dev` 启动日志已无该警告，HTTP 200，验证后停掉 dev server。

## Locate 定位：黄色冲击波 + 自动滚动 + 3s 脉冲

- 需求：点列表 Locate → 对应输出卡黄色冲击波特效 + 自动滚动 Execution Outputs(.exec-list 滚动容器)显示该卡；脉冲持续 3s，连续点击以最后一次起算 3s。
- App：新增 `locateTarget={id,n}` 状态(nonce)；`handleLocate(script)` 找该脚本最新 execution(find scriptId 匹配)，无则 alert 提示先 Execute；透传 ExecutionPanel。
- ExecutionPanel：透传 locateTarget 给 ExecutionCard。
- ExecutionCard：`cardRef` 引用 article；`useEffect([locateTarget,exec.id])` 命中则 `setPulse(true)` + `cardRef.scrollIntoView({behavior:'smooth',block:'center'})` + `setTimeout(setPulse(false),3000)`；effect cleanup 清旧 timer → 连续点击重置为最后一次 3s。flash/shock 用 `key={locateTarget.n}` 强制重挂载 → 动画每次重播。
- styles.css：`.exec-card` 加 `position:relative;overflow:hidden`；新增 `.exec-card__flash`(inset 覆盖层, inset 阴影避免被 overflow 裁剪) + `@keyframes shock-flash`(1s ease-in-out 3 = 3s 内闪 3 次)；`.exec-card__shock` 双环扩散；移除旧 `.is-located`/`shock-glow`。
- 验证：`npm run dev` 启动 HTTP 200，ExecutionCard/styles 编译 200，产物含 exec-card__flash/shock-flash/ease-in-out 3，旧 is-located 已移除；端口已释放。

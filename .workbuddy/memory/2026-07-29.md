# 2026-07-29

## App Info 增加 GitHub 仓库链接
- 在 App Info 弹窗（client/src/App.jsx）的 Version 下方新增 "Repository" 行，展示仓库地址 `https://github.com/bynow2code/easy-ops` 并作为可点击链接。
- 仓库地址定义为模块级常量 `GITHUB_REPO_URL`（App.jsx 顶部），避免硬编码散落。
- 打开方式：拦截默认 a 标签行为，Electron 下走 `window.electronAPI.openExternal`（新增 IPC），由主进程 `shell.openExternal` 用系统默认浏览器打开；纯网页调试回退 `window.open(..., '_blank')`。
- 新增 IPC：`electron/main.js` 注册 `ipcMain.handle('open-external', ...)`，仅放行 http/https；`electron/preload.js` 暴露 `openExternal`。
- App.css 新增 `.info-link` 样式（accent 色、可换行、hover 下划线）。
- 注：本沙箱 safe-delete trash 二进制缺失，vite build 的 emptyDir 阶段报错，但 403 模块均成功 transform，源码编译无误；真实环境正常 build 即可。

## 项目 README 多语言 + LICENSE
- 新增 `README_EN.md`（英文版，完整翻译自中文 README）。
- 在 `README.md`（中文，默认）与 `README_EN.md` 顶部各加语言切换链接行：`<a href="README.md">简体中文</a> · <a href="README_EN.md">English</a>`。GitHub 无原生语言切换，采用链接跳转方式；README.md 为仓库默认展示（简体中文）。
- 中文 README 末尾补「许可证」段链接到 LICENSE；英文版同样补 License 段。
- 新增 `LICENSE`（MIT，版权 `bynow2code`，年份 2026）。
- `package.json` 增加 `"license": "MIT"`。

## 定位窗口黄色特效增强
- 之前 Locate 特效仅让 group-badge 在淡黄/琥珀间轻微脉动 + 弱光晕，FE 徽标本就是黄色，几乎看不出来。
- 增强两处：
  1. `client/src/App.css` 的 `.group-badge.running`：`traffic-yellow-flash` 改为鲜金(#faad14)→亮金黄(#ffd666)强对比脉冲，叠加 `transform: scale(1.3)` 放大 + 强金色光晕（box-shadow 20px/6px/0.95），周期 1s。
  2. 新增 `.output-panel.locating` 与 `.modal-content.modal-maximized.locating` 的 `panel-locate-flash`：整块面板黄色描边脉冲(2→3px) + 整体染暖黄底色(rgba(250,173,20,0.12)) + 外发光，一眼锁定目标面板。
- `client/src/App.jsx`：output-panel 与 modal-maximized 的 className 增加 `locating` 条件类（locatingId === script.id / maximizedScriptId）；同时修正几处把黄色特效误写作「绿色闪烁」的注释。
- 触发逻辑不变：点击 Locate → scrollToOutput → 3s 后 locatingId 清空，动画自动停止。

## 全项目语法/逻辑/死代码检查与修复
- 检查手段：`node --check`（server/index.js、electron/main.js、electron/preload.js 全过）；ESLint 扫 client/src；esbuild 打包 server 成功(1.1mb)；启动打包后 server 冒烟测试 /api/scripts、/api/shells、/api/system-info 均正常（win32 上正确探测到 WSL/bash）。
- 修复的真实 bug：`client/src/components/Markdown.jsx` —— react-markdown v10 已移除 `<ReactMarkdown>` 的 `className` prop，原代码把 `className="markdown-body …"` 直接传给它导致被忽略、GitHub release 笔记丢失 github-markdown-css 样式；改为外层 `<div className="markdown-body …">` 包裹。顺手解构掉自定义 `a` 组件的 `node` 避免 React unknown-prop 警告。
- 修复的 lint 问题：`client/src/App.jsx` 空 `catch {}`(no-empty) 加注释；`useLayoutEffect` 补稳定的 `pinToBottom` 依赖；卸载时关闭全部 EventSource 的「读最新 ref」写法加说明性 eslint-disable。
- `client/eslint.config.js`：no-unused-vars 增加 `ignoreRestSiblings:true` + `argsIgnorePattern:'^_'`，支持 react-markdown 的 rest 解构惯用法。
- 结论：server/electron/ShellEditor 经人工审查无死代码、未用符号与逻辑 bug；端口分配、Shell 探测、SSE 流式、强杀、macOS 自研更新、单实例锁、退出清理均健全。vite build 仅因沙箱 safe-delete trash 缺失在 emptyDir 阶段失败，403 模块全部 transform 成功，源码可编译。
